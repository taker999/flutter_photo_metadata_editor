void downloadSvg(String svgContent) {
  // For mobile, this function can be left empty or handle the case appropriately
  // as downloading directly is not feasible. You might use platform-specific
  // solutions like sharing the SVG content or saving it to a file.
  print("Downloading SVG is not supported on mobile.");
}
